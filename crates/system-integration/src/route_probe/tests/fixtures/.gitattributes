# 真机抓取样本**逐字入库**：关掉 git 的一切行尾/编码转换。
#
# 仓根 .gitattributes 是 `* text=auto eol=lf`。对 Windows 的 `route print` 抓取，那条规则会在
# 入库时把 CRLF 规范成 LF —— 于是夹具里再也见不到 `\r`，而解析器在真机上读到的**就是** CRLF。
# 「照真实抓取写解析器」这条纪律会被这一步悄悄掏空：夹具绿、真机红，且没有任何地方会喊。
#
# 同理 macOS 抓取里的行尾空格（`netstat -rn` 的 Expire 列为空时留下的那几个空格）也必须原样留着。
*.txt -text
